
  # BeThat Space | UI/UX

  This is a code bundle for BeThat Space | UI/UX. The original project is available at https://www.figma.com/design/sZiRDP1LzBFbpS9EepX3ip/BeThat-Space-%7C-UI-UX.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  